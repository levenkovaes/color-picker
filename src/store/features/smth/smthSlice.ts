import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

export const smthSlice = createSlice({
  name: "people",
  initialState,
  reducers: {},
});

export const smthSliceReducer = smthSlice.reducer;
