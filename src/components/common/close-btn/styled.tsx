import styled from "styled-components";

import { SBtn } from "../styled";

export const SCloseBtn = styled(SBtn)`
  transform: scale(1.5);
  padding: 6px;
  line-height: 1;
  color: var(--dark-green);
`;
